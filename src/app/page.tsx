'use client';

import Pagination from '../components/Pagination';

const Home = () => {
  return (
    <div>
      <h1 className='text-base text-transparent'>User List</h1>
      <Pagination />
    </div>
  );
};

export default Home;
